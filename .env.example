# Copiar a .env.development / .env.test / .env.staging / .env.production
# segun corresponda. Nunca commitear archivos .env reales al repositorio.

NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=taxi_app
DB_PASSWORD=changeme
DB_DATABASE=taxi_pilot_dev

REDIS_HOST=localhost
REDIS_PORT=6379

JWT_SECRET=changeme-genera-un-valor-largo-y-aleatorio
